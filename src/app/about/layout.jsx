"use client"

export const metadata = {
    title: 'about us',
    description: 'Discover more about us',
}


export default function layout({ children }) {
  return (
    <div>
        {children}
    </div>
  )
}
